version: 0.2

phases:
  build:
    commands:
      - echo "Simple build: copy static-site to build-output"
      - mkdir -p build-output
      - cp -r static-site/* build-output/ || true

artifacts:
  files:
    - "**/*"
  base-directory: build-output
